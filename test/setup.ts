// Vitest setup file
import { beforeAll, afterEach, afterAll } from 'vitest';

beforeAll(() => {
  // テスト開始前の設定
});

afterEach(() => {
  // 各テスト後のクリーンアップ
});

afterAll(() => {
  // 全テスト終了後のクリーンアップ
});
